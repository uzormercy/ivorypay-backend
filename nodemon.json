{
    "watch": ["app"],
    "ext": "ts,js,json",
    "ignore": ["node_modules", "coverage", "dist"],
    "exec": "ts-node -r tsconfig-paths/register ./app/server.ts",
    "restartable": "rs",
    "env": {
        "NODE_ENV": "development"
    }
}